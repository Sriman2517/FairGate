# Server-only. This URL is a development default; production requires an explicit origin.
FAIRGATE_API_URL=http://127.0.0.1:4000
# Production requires HTTPS unless an explicitly protected private API connection is used.
# FAIRGATE_ALLOW_HTTP_API=true
