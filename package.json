{
  "name": "fairgate",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/api"
  ],
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "npm run dev --workspace @fairgate/api",
    "typecheck": "npm run typecheck --workspace @fairgate/api",
    "build": "npm run build --workspace @fairgate/api",
    "start": "npm run start --workspace @fairgate/api"
  }
}
